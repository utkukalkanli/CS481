if numpy module gives an error, try;
    pip3 install numpy --user

On global alignment, please consider the fact that there are more than 1 possible alignments but their score is the same.

To run, nothing special is needed. No need to compile python code to run it. "make all" command runs the code with the example given in hw4.pdf

It assumes 2 DNA sequence with equal line count is given as input
if numpy module gives an error, try;
    pip3 install numpy --user

To run, nothing special is needed. No need to compile python code to run it. "make all" command runs the code with the example given in hw6.pdf

It alignes multiple sequences and outputs the distance matrix of given sequences